var myLibrary = function() {
/**
 * calculates the area of a rectangle based of it's width and height
 * @param width {number}
 * @param height {number}
 * @returns the area of the rectangle
 */
}

function rectangleArea(width, height) {
    return width * height;
}


/**
 * calculates the area of a triangle based on it's base and height
 * @param base {number}
 * @param base {number}
 * @returns base and height of the triangle
 */

function triangleArea(base, height) {
    return base * height / 2;
}

function circleArea(radius) {
    return Math.PI * radius * radius;
}
